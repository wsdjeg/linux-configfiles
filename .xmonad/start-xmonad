# Xorg config
$HOME/.bin/xorg-keyboard &
$HOME/.bin/xorg-touchpad-off &
$HOME/.bin/xorg-monitors-one &

# Wallpaper
# TODO: Get a solid color.
nitrogen --restore &

# Finally, start xmonad
exec xmonad-session
